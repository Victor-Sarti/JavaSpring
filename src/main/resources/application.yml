cors:
  originPatterns: http://localhost:8080,http://localhost:3000,https://github.com/Victor-Sarti
file:
  upload-dir: /uploadDir
spring:
  application:
    name:JavaSpring
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/spring_boot_estud?useTimezone=true&serverTimezone=UTC
    username: root
    password: Sarti007@
  jpa:
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
    show-sql: false
    open-in-view: false
  servelet:
    multipart:
      enable: true
      file-size-threshold: 2KB
      max-file-size: 200MB
      max-request-size: 215MB

  logging:
    level:
   # root: WARN
    br.com.sarti: DEBUG